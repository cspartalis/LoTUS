"""
This file contains the mlflow tracking uri.
If MLflow is not running on the localhost, change the uri accordingly.
If MLflow is running on a different port (default is 5000) change the port accordingly.
(For lab environment -server 106-, the port is 8000)
(For lab environment -server 87-, the port is 5000)
"""

# mlflow_tracking_uri = "http://127.0.0.1:8000/"
mlflow_tracking_uri = "http://127.0.0.1:5000/"
